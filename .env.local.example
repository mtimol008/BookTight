# Copy this file to .env.local and fill in your Supabase project's values.
# Find these in the Supabase dashboard: Settings > API.
#
# The "anon" key is safe to expose in browser code (that's what NEXT_PUBLIC_
# means) — it only grants what your Row Level Security policies allow.
# Never put the "service_role" key in a NEXT_PUBLIC_ variable.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Grants full admin access — bypasses Row Level Security entirely. Server-only,
# used just to delete a user's auth account when they delete their Booktight
# account. Never expose this to the browser, never prefix it NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=

# Mapbox Geocoding API — find/create a token at https://account.mapbox.com/access-tokens/
# Deliberately NOT prefixed with NEXT_PUBLIC_: geocoding runs in server actions
# only, so this key never reaches the browser.
MAPBOX_ACCESS_TOKEN=
